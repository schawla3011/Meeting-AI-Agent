services:
  - type: web
    name: antigravity-meeting-recorder
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false   # paste value in Render dashboard
      - key: RESEND_API_KEY
        sync: false   # get free key at resend.com — primary email provider
      - key: SMTP_EMAIL
        sync: false   # Gmail address — local dev fallback only
      - key: SMTP_PASSWORD
        sync: false   # Gmail App Password — local dev fallback only
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /health
